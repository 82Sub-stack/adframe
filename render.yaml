services:
  - type: web
    name: adframe
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build && cd server && npx puppeteer browsers install chrome
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
